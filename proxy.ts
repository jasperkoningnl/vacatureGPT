import { NextRequest, NextResponse } from "next/server"; import { jwtVerify } from "jose";
export async function proxy(req:NextRequest){ if(req.nextUrl.pathname.startsWith("/login")||req.nextUrl.pathname.startsWith("/_next")) return NextResponse.next(); const secret=process.env.SESSION_SECRET; const password=process.env.APP_PASSWORD; if(!secret||!password) return new NextResponse("Applicatie is niet veilig geconfigureerd.",{status:503}); const token=req.cookies.get("vacature_session")?.value; try{ if(!token) throw new Error(); await jwtVerify(token,new TextEncoder().encode(secret)); return NextResponse.next(); }catch{return NextResponse.redirect(new URL("/login",req.url));} }
export const config={matcher:["/((?!favicon.ico).*)"]};
