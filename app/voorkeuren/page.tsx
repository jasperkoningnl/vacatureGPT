import { desc } from "drizzle-orm";import { getDb } from "@/lib/db";import { preferences } from "@/lib/db/schema";import { savePreferences } from "../actions";export const dynamic="force-dynamic";export default async function Page(){const [p]=await getDb().select().from(preferences).orderBy(desc(preferences.updatedAt)).limit(1);if(!p)return <p>Voer eerst de seed uit.</p>;return <><h1>Voorkeuren</h1>{p.salaryMin===null&&<p className="warn"><b>Minimumsalaris ontbreekt.</b> Vul dit nog in; vacatures zonder salaris blijven eerlijk als onbekend getoond.</p>}<form action={savePreferences} className="panel form"><input type="hidden" name="id" value={p.id}/><div className="filters"><label>Uren vanaf<input type="number" name="hoursMin" defaultValue={p.hoursMin}/></label><label>Uren tot<input type="number" name="hoursMax" defaultValue={p.hoursMax}/></label><label>Minimumsalaris<input type="number" name="salaryMin" defaultValue={p.salaryMin??""} placeholder="Nog invullen"/></label></div><label>Primaire steden<input name="primaryCities" defaultValue={p.primaryCities.join(", ")}/></label><label>Secundaire steden<input name="secondaryCities" defaultValue={p.secondaryCities.join(", ")}/></label><div className="filters"><label>Vertrekpunt<input name="travelOrigin" defaultValue={p.travelOrigin}/></label><label>Max. reistijd (min.)<input type="number" name="maxTravelMinutes" defaultValue={p.maxTravelMinutes}/></label></div><label>Rolfamilies<textarea name="roleFamilies" defaultValue={p.roleFamilies.join(", ")} rows={3}/></label><label>Positieve indicatoren<textarea name="positiveIndicators" defaultValue={p.positiveIndicators.join(", ")} rows={3}/></label><label>Negatieve indicatoren<textarea name="negativeIndicators" defaultValue={p.negativeIndicators.join(", ")} rows={3}/></label><button>Voorkeuren opslaan</button></form></>}
