import { count, desc, eq, gte } from "drizzle-orm"; import { getDb } from "@/lib/db"; import { feedback, sourceRuns, sources, vacancies } from "@/lib/db/schema";
export const dynamic="force-dynamic"; export default async function Home(){const db=getDb(); const week=new Date();week.setDate(week.getDate()-7); const [[active],[recent],runs,notes]=await Promise.all([db.select({n:count()}).from(vacancies).where(eq(vacancies.active,true)),db.select({n:count()}).from(vacancies).where(gte(vacancies.firstSeenAt,week)),db.select({name:sources.name,status:sourceRuns.status,finished:sourceRuns.finishedAt,count:sourceRuns.resultCount,warnings:sourceRuns.warnings}).from(sourceRuns).innerJoin(sources,eq(sourceRuns.sourceId,sources.id)).orderBy(desc(sourceRuns.startedAt)).limit(5),db.select({value:feedback.value,note:feedback.note,title:vacancies.title,updated:feedback.updatedAt}).from(feedback).innerJoin(vacancies,eq(feedback.vacancyId,vacancies.id)).orderBy(desc(feedback.updatedAt)).limit(5)]); const last=runs[0];return <><h1>Overzicht</h1><div className="grid"><div className="card"><span className="muted">Actieve vacatures</span><div className="metric">{active.n}</div></div><div className="card"><span className="muted">Nieuw deze week</span><div className="metric">{recent.n}</div></div><div className="card"><span className="muted">Brongezondheid</span><div className="metric">{last?.status??"—"}</div></div><div className="card"><span className="muted">Laatste inname</span><div className="metric" style={{fontSize:16}}>{last?.finished?.toLocaleString("nl-NL")??"Nog niet"}</div></div></div><div className="split" style={{marginTop:20}}><section className="panel"><h2>Bronruns</h2>{runs.map((r,i)=><p key={i}><b>{r.name}</b> · {r.status} · {r.count} resultaten {r.warnings.length>0&&<span className="warn">{r.warnings.length} waarschuwing(en)</span>}</p>)}</section><section className="panel"><h2>Recente feedback</h2>{notes.length?notes.map((n,i)=><p key={i}><b>{n.title}</b><br/><span className="muted">{n.value}{n.note?` · ${n.note}`:""}</span></p>):<p className="muted">Nog geen feedback.</p>}</section></div></>}
