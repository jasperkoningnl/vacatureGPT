import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";import { parseDetail,parseRss } from "./oneworld";const fixture=(name:string)=>readFileSync(new URL(`./fixtures/${name}`,import.meta.url),"utf8");describe("OneWorld parser",()=>{it("leest de publieke RSS zonder netwerk",()=>{const jobs=parseRss(fixture("oneworld-rss.xml"));expect(jobs.length).toBeGreaterThan(1);expect(jobs[0].url).toMatch(/^https:\/\/www\.oneworld\.nl\/job\//)});it("normaliseert JSON-LD detaildata zonder iets te verzinnen",()=>{const x=parseDetail(fixture("oneworld-detail.html"),"https://www.oneworld.nl/job/test/");expect(x.title).toBe("Campagnestrateeg Fondsenwerving");expect(x.location).toBe("Almere");expect(x.hoursMin).toBe(32);expect(x.hoursMax).toBe(36);expect(x.deadline).toBeInstanceOf(Date);expect(x.contentHash).toHaveLength(64)})});
